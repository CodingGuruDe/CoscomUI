import BaseDirective from '@coscom/coscom-ui/basedirective';
import TooltipStyle from '@coscom/coscom-ui/tooltip/style';

const BaseTooltip = BaseDirective.extend({
    style: TooltipStyle
});

export default BaseTooltip;
